module Reactimate.Game.Environment where

import Reactimate.Game.Assets (Assets)
import Reactimate.Time (Time)
import SDL qualified

-- data GameEnv = GameEnv
--   { window :: !SDL.Window,
--     renderer :: !SDL.Renderer,
--     assets :: !Assets,
--     time :: !Time
--   }
